<p style="color: green"><%= notice %></p>

<%= render @booking %>

<div>
  <%= link_to "Back to bookings", bookings_path %>
    <%= link_to "Update booking", edit_booking_path(@booking) %>
  <%= button_to "Destroy this booking", @booking, method: :delete, data: {turbo: false} %>
</div>
