<p style="color: green"><%= notice %></p>

<%= render @booking %>

<% if current_user.has_role?(:trainer, @booking) %>
  <div>
    <%= button_to "Cancel this booking", @booking, method: :delete, data: {turbo: false} %>
  </div>
<% end %>

<%= link_to "Back to my bookings", mybookings_path, class: "btn btn-primary" %>